# RAG Application — pinned to the working environment (Python 3.11)
# Install with:  pip install -r requirements.txt
#
# This app is fully local (Ollama + ChromaDB + a local reranker) and never calls
# OpenAI, so no OpenAI/llama-index-openai packages are required. Versions below
# match the tested myenv; `pip check` is clean for the packages the app imports.

# ---------------------------------------------------------------------------
# UI
# ---------------------------------------------------------------------------
streamlit==1.38.0
streamlit-pdf-viewer==0.0.18
altair==5.3.0
pydeck==0.9.1

# ---------------------------------------------------------------------------
# LlamaIndex — core + the two integrations the app actually imports
# ---------------------------------------------------------------------------
llama-index-core==0.14.22
llama-index-embeddings-ollama==0.9.0     # provides OllamaEmbedding (index_docs.py)
llama-index-vector-stores-chroma==0.5.5

# ---------------------------------------------------------------------------
# Vector store
# ---------------------------------------------------------------------------
chromadb==1.5.9
onnxruntime==1.19.2

# ---------------------------------------------------------------------------
# Reranker
# ---------------------------------------------------------------------------
sentence-transformers==3.1.1
transformers==4.44.2
huggingface-hub==0.25.2
tokenizers==0.19.1

# ---------------------------------------------------------------------------
# PDF processing
# ---------------------------------------------------------------------------
pymupdf==1.24.11
pillow==10.4.0
pytesseract==0.3.13

# ---------------------------------------------------------------------------
# Core shared deps — pinned to match the working environment
# ---------------------------------------------------------------------------
httpx==0.27.2
httpcore==1.0.5
anyio==4.6.2
pydantic==2.13.4
pydantic-core==2.46.4
typing-extensions==4.15.0
tenacity==8.5.0
tqdm==4.66.5
numpy==1.26.4
pandas==2.2.3
pyarrow==17.0.0
requests==2.32.3
urllib3==2.2.3
certifi==2024.8.30
charset-normalizer==3.4.0
idna==3.10
attrs==24.2.0
packaging==24.1
fsspec==2024.9.0
filelock==3.16.1
